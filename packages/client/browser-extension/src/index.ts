/** Node half of the browser-extension provider plugin; browser behavior lives in ./client. */

/** Cordis plugin name. */
export const name = 'client-browser-extension'

/** Host half intentionally has no runtime behavior. */
export function apply(): void {}
